apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flask-app.fullname" . }}
  namespace: flask-app
  labels:
    {{- include "flask-app.labels" . | nindent 4 }}
data:
  APP_NAME: "devops-project-2"
  APP_ENV: "dev"
  LOG_LEVEL: "INFO"